<div class="bg-white rounded-xl shadow-md p-6 flex items-center justify-between">
  <div>
    <p class="text-gray-500 font-medium"><%= title %></p>
    <p class="text-2xl font-bold mt-2 text-gray-800">
      <%= value %>
    </p>
  </div>

  <div class="<%= icon_bg %> <%= icon_color %> rounded-full p-4">
    <i class="<%= icon %> text-xl"></i>
  </div>
</div>
